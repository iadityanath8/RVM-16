.entry main 

add_fn:    
    push fp 
    mov fp, sp 
    
    load r1, [fp + 4]
    load r2, [fp + 6]
    add r1, r2
    mov sp, fp 
    pop fp
    ret 

main:
    push 1
    push 3
    call add_fn
    hlt

